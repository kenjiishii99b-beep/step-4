# アパレル POSアプリ 要件定義書（改訂版・完成版）

- **作成日**：2026年8月
- **作成者**：＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 1. 前提条件（変更不可）

### 1.1 システム構成
本システムは、Webアプリケーションとして構築し、各種デバイスのブラウザから利用する。
- **フロントエンド**：Next.js
- **バックエンド**：FastAPI
- **インフラ**：Microsoft Azure
- **データベース**：Azure Database for MySQL Flexible Server
- **利用方式**：Webブラウザ
- **バックエンド構成**：DB接続を維持する構成とし、コールドスタートによる応答遅延を可能な限り回避する。
- **認証方式**：ID・パスワード認証

### 1.2 利用環境
- カメラ付きデバイス（ノートPC・スマートフォン・タブレット）で利用する。
- バーコード読み取りは各デバイスのカメラを使用する。
- デバイスのWebブラウザから利用する。
- HTTPS通信を必須とする。

---

## 2. バーコード・SKU仕様

### 2.1 バーコード仕様
バーコードは **EAN-13（JANコード）** を採用する。
- EAN-13バーコードはSKUを一意に識別する。
- 1つのSKUに対して登録できるEAN-13バーコードは1件のみとする。
- EAN-13バーコードはSKUテーブルで一意に管理する。同一EAN-13を複数SKUに登録することは禁止する。
- 商品タグに記載されたバーコードはSKUと一致する前提とする。

### 2.2 SKU体系
SKUは「**商品ID（型番）＋サイズコード＋カラーコード**」によって構成する。
SKUは上記3要素の組み合わせによって一意に識別する。

**一意性の定義：**
- SKU ID：一意
- 商品ID ＋ サイズコード ＋ カラーコード：一意
- EAN-13：一意

### 2.3 サイズ・カラーコード
サイズ体系・カラー体系は商品カテゴリによって異なるため、マスター管理する。商品ごとに使用する体系を指定する。
- **サイズマスター**：サイズ体系ID / サイズコード / サイズ名称 / 表示順 / 有効・無効
- **カラーマスター**：カラー体系ID / カラーコード / カラー名称 / 表示順 / 有効・無効

---

## 3. 機能要求

### 3.1 バーコードスキャンによる商品登録
デバイスのカメラでEAN-13バーコードを読み取り、SKUを特定する。

**正常時のフロー：**
1. デバイスのカメラを起動
2. EAN-13を読み取る
3. バーコードからSKUを検索し、情報を取得
4. 購入リストへ登録
5. 「1件追加されました」等のフィードバックを表示

**SKU登録ルール：**
- **既に購入リストに存在するSKU**：数量を1加算
- **存在しないSKU**：新規行を追加
- **数量上限**：99 / **購入リスト上限**：100SKU
- **連続スキャン**：カメラ画面を閉じることなく連続読み取り可能とする。
- **読み取りエラー時**：バーコード認識不可、形式違い、未登録、無効なSKUなどの場合は、SKUコード（商品ID＋サイズコード＋カラーコード）の手入力を許可する。

### 3.2 購入リストの商品選択
購入リストからSKUを選択できる。選択された行は視覚的に強調表示する。
- **表示項目**：商品画像 / 商品名 / SKU / サイズ / カラー / 単価 / 数量 / 値引き情報 / 小計

### 3.3 購入リストの商品削除
選択中のSKUを購入リストから削除できる。削除前には確認ダイアログを表示する。

### 3.4 購入リストの数量変更
数量は1～99の範囲で設定可能とする。
- 数量0は設定不可（0相当の操作は削除扱いとする）。
- 数量変更後、小計・合計を即時再計算する。
- 数量99を超える場合はエラー表示とする。

### 3.5 会員特典・値引き
- **値引きの適用優先順位**：① SKU値引き ＞ ② 商品ID値引き ＞ ③ 会員値引き
  - 同一対象に複数の通常値引きが存在する場合は、優先順位の最も高いものを適用する。
- **値引き計算順序**：
  1. SKU値引き または 商品ID値引き
  2. 金額値引き
  3. 会員値引き
  4. 値引き後の税抜金額を算出
  5. 消費税を計算
- **値引き種別**：割引率（％） / 金額値引き（円）
- **仕様**：
  - 対象商品、期間、内容はDBで管理し、コード変更不要な構成とする。
  - 購入確定時点の値引き内容・値引き額を売上データとして保存する。マスター変更後も過去取引のデータは変更されない。

### 3.6 商品単価
- **単価優先順位**：SKU単価 ＞ 商品ID単価
  - SKU単価が未登録の場合は商品ID単価を使用する。
  - 購入確定時点の単価を売上明細に保存する。マスター変更後も過去取引の単価は変更されない。

### 3.7 レジ担当者ログイン
- 担当者ID＋パスワードによるログイン方式。
- ログイン専用画面を用意し、正常ログイン後はPOS販売画面へ遷移する（誤認証時はエラー表示）。
- SSO（シングルサインオン）は利用しない。

### 3.8 会員情報
- 会員IDはバーコードスキャンにより任意のタイミングで入力可能。未入力でも購入可能。
- 購入確定前に入力されている場合、会員特典を適用する。
- 存在しない会員IDの場合：エラーを表示し、会員なしとして処理（特典適用外）。
- 会員情報項目：会員ID / 氏名 / 電話番号 / 住所 / 性別 / 年齢

### 3.9 消費税
- 消費税率は決済確定日時を基準として判定する。
- 税計算方式：外税方式（1円未満の端数は切り捨て）。軽減税率は対象外。
- 税率マスター：税率 / 適用開始日時 / 適用終了日時 / 有効・無効
- 画面表示：購入確定前に税抜小計、値引き額、税抜合計、消費税額、税込合計を表示する。
- 保存仕様：購入確定時に適用税率・税額を売上データへ保存（マスター変更後も不変）。

### 3.10 購入確定・決済
購入確定時に以下の情報を確定し、売上・在庫データの整合性を保った状態で永続化する。
- 購入SKU / 数量 / 購入時単価 / 値引き額 / 税率 / 税額 / 税抜金額 / 税込金額 / 会員ID / スタッフID / 決済方法 / 購入日時

---

## 4. 画面要件

- **4.1 ログイン画面**：担当者ID入力 / パスワード入力 / ログインボタン / エラーメッセージ
- **4.2 販売画面**：
  - **商品検索・入力**：バーコード入力欄, カメラ起動, SKU手入力, 会員ID入力/読取
  - **商品一覧**：画像, 商品名, 価格, SKU, サイズ, カラー
  - **購入リスト**：SKU, 商品名, サイズ, カラー, 単価, 数量, 値引, 小計, 選択状態
  - **金額表示**：税抜小計, 値引合計, 消費税, 税込合計
  - **操作**：商品削除, 数量変更, 決済ボタン
- **4.3 商品詳細画面**：商品画像 / 商品名 / 商品ID / SKU / サイズ / カラー / 現行単価 / 購入時単価 / 店舗在庫 / 倉庫在庫
- **4.4 返品・交換画面**：元取引検索 / 元取引商品表示 / 返品商品再スキャン / SKU照合 / 返品・交換判定処理（記録項目: 元売上ID, 返品/交換SKU, 数量, 購入時単価/値引/税率/税額, 処理スタッフID, 処理日時, 取引種別）
- **4.5 在庫管理画面**：SKU一覧 / 店舗・倉庫在庫 / 入荷登録(バーコード) / SKU単位の店舗間移動 / 在庫移動履歴
- **4.6 会員管理画面**：会員検索 / 会員詳細 (氏名, 電話番号, 住所, 性別, 年齢, 購入履歴, ポイント)

---

## 5. データ要件（DB）

主要なエンティティとその保持項目を定義する。

- **5.1 商品マスター**：商品ID / 商品名 / カテゴリ / 現行単価 / 画像URL / サイズ体系ID / カラー体系ID
- **5.2 SKU**：SKU ID / 商品ID / バーコード(EAN-13) / サイズコード / カラーコード / 店舗在庫 / 倉庫在庫 / ロケーション
- **5.3 サイズマスター**：サイズ体系ID / サイズコード / サイズ名称 / 表示順 / 有効・無効
- **5.4 カラーマスター**：カラー体系ID / カラーコード / カラー名称 / 表示順 / 有効・無効
- **5.5 単価履歴**：単価履歴ID / 商品ID / SKU ID / 単価 / 適用開始・終了日時 / 有効・無効
- **5.6 値引きマスター**：値引きID / 対象区分 / 商品ID / SKU ID / 割引種別 / 割引値 / 適用開始・終了日時 / 優先順位 / 有効・無効
- **5.7 税率マスター**：税率ID / 税率 / 適用開始・終了日時 / 有効・無効
- **5.8 会員**：会員ID / 氏名 / 電話番号 / 住所 / 性別 / 年齢 / 購入履歴 / ポイント
- **5.9 スタッフ**：スタッフID / 担当者名 / パスワードハッシュ / 権限 / 有効・無効 / 作成・更新日時
- **5.10 売上・取引**：
  - **取引**：取引ID / 会員ID / スタッフID / 決済方法 / 税抜合計 / 値引合計 / 税額 / 税込合計 / 取引種別 / 取引日時
  - **売上明細**：売上明細ID / 取引ID / SKU ID / 商品ID / 数量 / 購入時単価 / 値引額 / 適用税率 / 税額 / 税抜・税込金額 / 元売上明細ID
- **5.11 在庫履歴**：在庫履歴ID / SKU ID / ロケーション / 変動数量 / 変動種別(入荷/販売/返品/移動/調整) / 元取引ID / 処理スタッフID / 処理日時

---

## 6. 非機能要求

### 6.1 パフォーマンス
- バーコード読み取り → SKU表示：0.5秒以内を目標
- 購入リスト更新：1秒以内
- 購入リストSKU数：最大100件 / SKU数量：1～99
- ※通信環境・デバイス性能・カメラ認識時間を除くサーバー処理を基本評価対象とする。

### 6.2 信頼性
- 購入確定時に売上データを永続化し、データ整合性を維持する。
- 売上データを意図せず削除できない構成とする。
- 過去取引の単価・値引き・税率・税額、在庫変動履歴、返品・交換履歴を永続的に保持する。

### 6.3 セキュリティ
- HTTPS通信必須。
- 会員情報の適切な保護、スタッフ権限管理の実施。
- パスワードは安全なハッシュ方式で保存（平文保存禁止）。
- 認証済みユーザーのみPOS機能を利用可能とし、権限に応じたアクセス制御を行う。

### 6.4 拡張性
- 将来的な拡張（ECサイト在庫連携API、店舗/SKU/カテゴリ/サイズ/カラーの追加、決済方法追加など）を考慮した設計とする。
- 値引き・税率・単価はDB管理とし、プログラム改修なしで変更できる構成とする。

---

## 7. 業務ルール

1. **SKU管理**：SKUを販売・在庫管理の基本単位とし、EAN-13バーコードで一意に識別する。
2. **単価**：SKU単価 ＞ 商品ID単価。購入時点の単価を売上明細に保存。
3. **値引き**：SKU値引き ＞ 商品ID値引き ＞ 会員値引き。購入確定時に明細へ保存。
4. **税率**：決済確定日時によって判定し、DBで期間管理する。
5. **返品・交換**：
   - 元取引SKUと再スキャンSKUを照合し、一致＝返品 / 不一致＝交換 とする。
   - 処理時には購入時の単価・値引き・税率・税額を使用する。

---

## 8. 業務フロー

### 8.1 通常販売フロー
```
[ログイン]
   ↓
[POS画面]
   ↓
[カメラでバーコードスキャン] → [SKU特定]
   ↓
[リスト追加・数量変更]
   ↓
[会員ID入力 (任意)]
   ↓
[値引き・税計算] → [金額表示]
   ↓
[決済] → [売上確定・在庫更新]
```

### 8.2 返品・交換フロー
```
[返品・交換画面]
   ↓
[元取引検索] → [元取引SKU確認]
   ↓
[返品商品再スキャン] → [SKU照合]
   ├── [一致]   → 返品処理
   └── [不一致] → 交換処理
   ↓
[購入時情報を使用して処理]
   ↓
[履歴保存・在庫更新]
```

---

## 9. 権限管理

| 機能 / メニュー | 一般 | 店長 | 管理者 |
| :--- | :---: | :---: | :---: |
| POS販売 | ○ | ○ | ○ |
| 返品・交換 | ○ | ○ | ○ |
| 在庫確認 | ○ | ○ | ○ |
| 入荷登録 | ○ | ○ | ○ |
| 店舗間移動 | × | ○ | ○ |
| 会員検索 | ○ | ○ | ○ |
| 会員管理 | × | ○ | ○ |
| 商品マスター | × | ○ | ○ |
| 値引きマスター | × | ○ | ○ |
| 税率マスター | × | × | ○ |
| スタッフ管理 | × | × | ○ |

---

## 10. 対象外・後続工程

以下は詳細設計の対象外とし、後続工程で定義する。
- API詳細仕様 / API認証方式の詳細
- 画面UIデザイン / 画面遷移図の詳細
- DB物理設計 / ER図
- Azureリソース詳細設計 / CI/CD詳細 / 監視・ログ基盤詳細
- ECサイト連携API詳細 / 決済サービスとの具体的な接続仕様

---

## 11. 要件定義上の重要事項

1. **SKU中心設計**：SKUを販売・在庫管理の基本単位とする。
2. **バーコード中心設計**：EAN-13＝SKUを一意に特定するコードとして扱う。
3. **マスターと取引データの分離**：商品や各種設定のマスター情報を変更しても、過去の取引データは変更されない。
4. **購入時点情報の保存**：売上明細には購入時点の単価・値引き・税率・税額をスナップショットとして保存する。
5. **カメラ活用POS**：バーコード読み取りはデバイス（ノートPC・スマホ・タブレット）のカメラを利用する。
6. **DBによるルール管理**：価格・値引き・税率等の変更は、原則DBのマスター変更で対応し、プログラム変更を不要とする。

---

## 12. 要件定義書まとめ

本アプリは、アパレル店舗における販売・在庫・会員・返品交換業務を対象としたマルチデバイス向け（ノートPC・スマホ・タブレット）Web POSシステムである。

本システムでは、「**EAN-13バーコード → SKU → 商品・価格・在庫・売上**」という関係を基本とする。SKUを販売・在庫管理の基本単位とし、サイズ・カラーをマスター管理する。

また、単価・値引き・税率については購入時点の情報を売上データとして保持することで、マスター変更後も過去取引の正確性を維持する。

返品・交換については、元取引のSKUと再スキャンしたSKUを照合し、SKU一致の場合は返品、不一致の場合は交換として処理する。

システム構成は、「**Next.js → FastAPI → Azure Database for MySQL Flexible Server**」を基本構成とし、Azure上で運用可能な拡張性・信頼性・セキュリティを考慮した構成とする。

以上を本システムのLv1＋Lv2統合要件とする。
