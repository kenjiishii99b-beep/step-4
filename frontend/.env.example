# `cp .env.example .env.local` して値を調整すること。
#
# BACKEND_INTERNAL_URL はサーバー側（BFF: src/app/api/bff/**）だけが参照する内部URL。
# NEXT_PUBLIC_ を付けない = ブラウザ側 JS にバンドルされない = FastAPI の物理URLをクライアントに一切露出しない。
BACKEND_INTERNAL_URL=http://localhost:8000
